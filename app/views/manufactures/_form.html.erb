<%= render partial: 'shared/errors', locals: { record: manufacture } %>
<%= form_for manufacture do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <%= f.submit 'Enviar', class: 'btn btn-primary' %>
<% end %>
